#lang scheme
(define (p) (p))
(define (test x y)
  (if (= x 0)
      0
      y))
;applicative-order: -first get subexpression. -apply the leftmost operator to the operands. -> Infinite loop.
;normal-order: (test 0 (p)) expands to (if #t 0 (p)) => 0